# .env.example - Örnek Environment Dosyası
# Bu dosyayı sunucuda .env olarak kopyalayın ve değerleri güncelleyin

# Flask Ortam Ayarı
FLASK_ENV=production

# Flask Gizli Anahtarı (ZORUNLU!)
# Güçlü bir anahtar oluşturmak için: python3 -c "import secrets; print(secrets.token_hex(32))"
FLASK_SECRET_KEY=buraya-guclu-bir-anahtar-yazin-32-karakter-olmali

# Port (opsiyonel, varsayılan: 5000)
PORT=5000
